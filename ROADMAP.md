# AEGIS — Roadmap

Этот файл отвечает на один вопрос: **что готово и что делать дальше.**

- Позиционирование — [`docs/PRODUCT.md`](./docs/PRODUCT.md).
- Право выпускать 1.0 — [`RELEASE-CHECKLIST.md`](./RELEASE-CHECKLIST.md).
- Рабочие заметки и история находок — `handoff.md`.

Статус: ✅ готово · 🟡 работает, есть что докрутить · ❌ нет

> **Ревизия 2026-09-23.** Файл переписан целиком. Прошлая версия занижала
> состояние в четырёх местах: вердикт «сильный MVP / технологическое демо»
> писался до закрытия всех P0, пункт P2-1 утверждал, что проверить печати
> снаружи нечем (эндпоинт существует с #76), P1-4 числил спеки под каждого
> клиента несделанными (они работают), а формат «абзац в ячейке таблицы» довёл
> файл до состояния, в котором его перестали читать. Каждый пункт ниже сверен с
> кодом 23.09 — файлом, маршрутом или функцией, а не памятью.

---

## 1. Где продукт сейчас

| Измерение | Состояние |
|---|---|
| **Код** | Поздний pre-1.0. Гейт релиза: 52 пункта закрыто, 16 наполовину, 4 открыто |
| **Продажи** | Нулевая стадия. 0 пилотов, 0 клиентов, 0 разговоров с покупателем |
| **Инфраструктура** | Регресс: своя кузница (Gitea) недоступна, CI на домашнем сервере остановлен |

**Вердикт.** Это больше не демо. Все пять блокеров релиза закрыты, ров
(детект BOLA/BFLA) построен, стадия Prove — подписанные документы, реестр
инцидентов, печати журнала — существует и проверяема снаружи. Это продукт без
покупателя, и ограничивает его не код.

Из четырёх открытых пунктов гейта два — не задачи, а честно записанные границы
(см. §7): у данных, питающих подписанный документ, нет защиты целостности, и
независимого свидетеля подписи не существует.

---

## 2. Что работает

### Шлюз
Reverse proxy, round-robin, circuit breaker, retry только для идемпотентных
методов · rate limiting на Redis · JWT (HMAC + JWKS, защита от alg-confusion,
fail-closed при недоступном JWKS) · DLP-маскирование ответов (стриминг,
WebSocket) · IP-guard, threat feed, поведенческий скоринг, challenge ·
WAF на Coraza со встроенными правилами **или** полным OWASP CRS v4
(`waf.use_crs`, paranoia 1–4, monitor/block) · hot-reload конфига с полным
гейтом валидации · настоящий JA3 из TLS ClientHello (`internal/tlsfp`), а
спуфящийся заголовок срезается.

Порядок цепочки — `internal/gateway/chain.go`, 22 шага, закреплён тестом
`TestChainSteps_OrderIsLoadBearing`.

### Слой API-безопасности
Пассивный каталог с нормализацией путей · posture (protected / partial /
unprotected / shadow) и риск-скоринг · аналитика потребителей · per-route
переопределение контролей · **BOLA/BFLA** (`AbuseDetection`): перебор объектов,
подтверждённое владение из тела ответа, проактивный блок до форварда, BFLA по
проверенным ролям · находки утечки данных (API3/API9) · GraphQL: каталог, BOLA
и привязка PII к операции.

### Режимы посадки
**Зеркало** (`mirror_sink`) — копия трафика, ответ отбрасывается, трафик
клиента от нас не зависит. **Observe** (`observe: true`) — inline, но ничего не
блокирует и не модифицирует. Оба существуют ради одного: первого пилота без
риска для клиента.

### Доказательства (Prove)
Подписанные отчёты (`?sign=1`, Ed25519, автономный `cmd/reportverify`) ·
маппинг на **NIS2** ст. 21(2), **DORA** ст. 8–10 и ISO 27001 · реестр
инцидентов (`internal/incident`) со сроками NIS2 ст. 23 и классификацией
DORA ст. 18 · печати журнала (почасовой корень Меркла, цепочка, подписанная
голова) · проверка печатей снаружи: `GET /api/forensic/seals`, подписываемая ·
каждый подписанный документ обязан перечислить, чего он **не** доказывает —
`writeSignable` отказывается подписывать документ без такого списка.

### Эксплуатация
Мультитенантность на 6/6 этапов (изоляция в PostgreSQL через RLS и в Redis
через префикс ключа, CRUD тенантов и пользователей) · вход в консоль по
email+паролю и по OIDC (PKCE, JIT-провижининг) · роли admin/viewer · аудит
действий администратора · удаление старых данных по расписанию · лицензии
Ed25519 с привязкой к железу, BUSL 1.1 · Prometheus `/metrics` · алертинг в
вебхук (https, через `safefetch`) · Helm chart, docker-compose.

---

## 3. Чего нет

| Чего нет | Оценка | Комментарий |
|---|---|---|
| Защита целостности `incidents` | 1–2 дня | Таблица прямо решает, что написано в **подписанном** отчёте. Подчистка стоит одного `DELETE` |
| Защита целостности `admin_audit_log` | вместе с предыдущим | Журнал инсайдера, у которого нет ни RLS, ни контроля целостности |
| Внешний якорь подписи | 1–2 дня | Только если спросит клиент — см. §7 |
| Поиск аномалий по поведению (P1-2) | 3–4 нед | Есть только baseline для BOLA |
| HA и опубликованные замеры скорости (P1-7) | 3–4 нед | Latency зашумлена сетью, чистого замера нет |
| Account Takeover (A3) | 2–3 нед | Отдельный сценарий с высокой ценностью |
| Шаблоны PCI-DSS / HIPAA / GDPR | 3–4 нед | Есть NIS2, DORA, ISO 27001 |
| Импорт схемы GraphQL | 2–3 нед | Без неё нет аналога API9 для GraphQL |
| SIEM-экспорт (Splunk/Elastic), тикетинг | 2–3 нед | Вебхук работает, остальное нет |
| SAML, SCIM, MFA | 2–3 нед | OIDC закрывает это у большинства клиентов через IdP |
| eBPF-сенсор | 4–8 нед | **Не нужен**, пока зеркало не встретило живого возражения |
| Независимый пентест | деньги | Scope написан, подрядчик не нанят |

---

## 4. Блокеры релиза (P0) — закрыты все пять

| # | Что было | Чем закрыто |
|---|---|---|
| P0-1 | Тестов почти нет | Покрытие ≥70% на всех критических пакетах, `scripts/coverage-gate.sh` блокирует падение в CI, тесты идут под ролью без права обходить RLS |
| P0-2 | Нет аутентификации консоли | `internal/iam` (bcrypt, сессии, CSRF, роли) + `internal/sso` (OIDC) |
| P0-3 | Нет мультитенантности | 6 этапов ADR-001 закрыты: RLS в PostgreSQL, префикс ключей в Redis, CRUD тенантов, нагрузочный прогон |
| P0-4 | JA3 фиктивный | Фингерпринт из ClientHello, спуфящийся заголовок срезается |
| P0-5 | Fail-open при падении Redis | `fail_closed` для rate limit и IP-guard; скоринг поведения намеренно остаётся fail-open |

Осталось из P0 только то, что не закрывается кодом: **независимый пентест**.

---

## 5. Нужно для сделок (P1)

| # | Что | Статус |
|---|---|---|
| P1-1 | Детект OWASP API Top-10 | 🟡 BOLA/BFLA v2 и утечки данных готовы; mass assignment закрыт схемой; injection и broken-auth velocity — нет |
| P1-2 | Поиск аномалий | ❌ Только baseline под BOLA |
| P1-3 | Интеграции и алертинг | 🟡 Вебхук (generic/slack, порог важности); SIEM и тикетинг — нет |
| P1-4 | OpenAPI и дрейф спеки | 🟡 Импорт, дрейф, enforcement по схеме и спеки под каждого тенанта работают; нет форматов, bounds и автогенерации спеки из трафика |
| P1-5 | Модели развёртывания | 🟡 Зеркало и observe готовы; интеграции с Kong/Apigee/AWS API GW — нет |
| P1-6 | Хранение и объём данных | 🟡 Удаление по расписанию работает; свёртка старых данных и бэкапы — нет |
| P1-7 | HA и масштаб | ❌ Не начато |

---

## 6. Дифференциация (P2)

| # | Что | Статус |
|---|---|---|
| P2-1 | Отчётность для регуляторов | 🟡 NIS2 / DORA / ISO, подписанные отчёты, реестр инцидентов, печати журнала и их внешняя проверка. Нет: PCI/HIPAA/GDPR, экспорт PDF, страница инцидентов в консоли |
| P2-2 | Лицензирование | 🟡 Подписанные лицензии, привязка к железу, grace-период, переиздание, тарифные гейты. Нет: учёт фактического потребления, отзыв |
| P2-3 | Зрелость WAF | 🟡 CRS v4 с anomaly scoring и monitor-режимом. Нет: управление ложными срабатываниями из UI |
| P2-4 | Зрелость консоли | 🟡 Все основные страницы есть. Нет: временные диапазоны, пагинация, нормальные графики |
| P2-5 | Документация | 🟡 Есть runbooks, ADR, доки на режимы. Нет: upgrade path, SLA |

---

## 7. Границы, которые нельзя переобещать

Это не задачи, а свойства продукта. Они записаны здесь, чтобы ни один документ
и ни одна продажа не утверждали больше, чем код доказывает.

1. **Печати делают правку журнала обнаружимой, а не невозможной.** Правильное
   слово — tamper-**evident**. Удаление внутри периода ломает корень, обрезка
   хвоста ловится подписанной головой цепочки, но оператор с ключом может
   переписать цепочку и переподписать голову.
2. **Ключ подписи держит тот, кого проверяют.** Подпись доказывает «документ не
   изменён после создания», но не «документ собран не из подчищенных данных».
   Независимого свидетеля в схеме нет, и это закрывается только внешним якорем
   (RFC 3161 или прозрачный журнал).
3. **Данные, питающие подписанный документ, не защищены.** `incidents` — самый
   прямой случай: удаление строки меняет то, что подписанный отчёт говорит про
   DORA ст. 17–19, и не обнаруживается ничем. См. §3.
4. **Зеркало не видит ответов.** Значит нет ни находок PII, ни BOLA, ни
   статусов, ни латентности. Ноль в `pii_count` читается как «ответы не
   смотрели», а не «чисто» — так это и помечено в коде.
5. **Статическая находка — не сработавший детект.** В маппинге на DORA ст. 10(1)
   засчитываются только наблюдённые события; находка говорит, что эндпоинтом
   *можно* злоупотребить, а не что злоупотребление поймано.

---

## 8. Порядок работы

Приоритет пункта = стадия воронки `See → Protect → Prove`, на которой
застревает сделка (PRODUCT.md §3).

**Сейчас, в порядке окупаемости:**

1. **Первый пилот.** Единственное, что ограничивает проект. Материалы готовы
   (`docs/sales/`), режим, снимающий главное возражение, готов.
2. **Целостность `incidents` и `admin_audit_log`** — 1–2 дня одним механизмом.
   Последнее место, где продукт утверждает больше, чем доказывает.
3. **Вернуть CI** — свой сервер и очередь прогонов. Пока его нет, единственная
   проверка — `make preflight` локально.
4. **Чистый замер оверхеда** — час работы; «плюс N мс» снимает половину
   возражений на звонке.

**По запросу клиента, не раньше:** внешний якорь · PCI/HIPAA/GDPR · SIEM ·
SAML/SCIM/MFA · интеграции с Kong/Apigee.

**Не делать:** мажорные миграции фронтенда (React 19, Vite 8, Tailwind 4 —
закрыты с причиной) · eBPF-сенсор, пока зеркало не встретило возражения.

---

## 9. Открытые вопросы, которые не решаются кодом

- Целевой сегмент: SMB self-hosted или enterprise SaaS?
- Модель поставки: self-managed (Helm) или managed?
- **Платит ли кто-нибудь за это.** Позиционирование за одну сессию менялось
  трижды, и ни одна смена не была вызвана фактом извне. Опровергнуть или
  подтвердить тезис о рынке нельзя, не отправив первое письмо.

---

## 10. Длинный горизонт

Оценки грубые и предполагают одного опытного разработчика. Фазы A–D в одиночку
— около года плотной работы, E–F добавляют столько же. Поэтому порядок важнее
полноты: каждый доведённый пункт даёт ценность сам по себе.

| Фаза | Что |
|---|---|
| **A. Детект** | A1 BOLA/BFLA ✅ · A2 baseline 🟡 · A6 контроль ложных срабатываний 🟡 · A3 Account Takeover ❌ · A4 бизнес-логика ❌ · A5 аномальные последовательности ❌ |
| **B. Охват** | B1 зеркало ✅ / eBPF ❌ отложен · B2 OpenAPI 🟡 · B5 GraphQL 🟡 · B3 мульти-источники ❌ · B4 zombie/версии ❌ |
| **C. Данные** | C1 классификация 🟡 (карты с Luhn, SSN, email, телефоны → PCI/PII) · C2 lineage ❌ · C3 PCI/HIPAA/GDPR ❌ |
| **D. Встраивание** | D2 алертинг 🟡 · D3 Prometheus ✅ · D1 SIEM/SOAR ❌ · D4 авто-ответ ❌ · D5 проверка на PR ❌ |
| **E. Enterprise** | E1 мультитенантность ✅ · E2 OIDC ✅ / SAML+SCIM+MFA ❌ · E3 аудит 🟡 · E4 лицензии 🟡 · E5 SOC2/ISO ❌ (процесс, не код) |
| **F. Масштаб** | F4 покрытие тестами ✅ · F1 HA ❌ · F2 опубликованные бенчмарки ❌ · F3 распределённый control plane ❌ |
